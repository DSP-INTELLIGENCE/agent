# Ground Bridge Checklist

Do not start until patcher report is fixed.

Requirements:

```text
[ ] EvidencePacket created/stored
[ ] registry["last_evidence_packet"] set
[ ] packet.render_answer_context() used
[ ] /grounding packet-first
[ ] /sources packet-first
[ ] no raw prompt fallback
[ ] legacy display fallback only
```
