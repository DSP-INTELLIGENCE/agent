# Codex Operator Handoff

You are operating in `~/Downloads/agent`.

Use `codec-patch.py`:

```text
review -> inspect report -> publish -> merge-cleanup
```

Stop conditions:

```text
dirty worktree
failed preflight
failed apply
failed tests
unexpected changed files
report misses new/untracked files
```

Do not revive:

```text
/question /rag /research legacy prompt templates semantic router execution AgentSpec execution AgentScript execution
```
